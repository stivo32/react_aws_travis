language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t stivo32/react_aws_travis -f Dockerfile.dev .

script:
  - docker run -e CI=true stivo32/react_aws_travis npm run test